id: core/edit
kind: tool
description: Edit a file inside the local workspace
inputSchema:
  type: object
  properties:
    path:
      type: string
    old:
      type: string
    new:
      type: string
  required:
    - path
    - old
    - new
execution:
  mode: host
  operation: edit
risk:
  level: medium

id: web/fetch
kind: tool
description: Fetch the contents of a URL for analysis
inputSchema:
  type: object
  properties:
    url:
      type: string
  required:
    - url
execution:
  mode: http
  operation: web-fetch
risk:
  level: low
